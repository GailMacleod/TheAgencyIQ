delete vite.config.ts to remove Vite. Open package.json, remove 'vite' from 'devDependencies' or 'dependencies' if present, and update scripts to remove vite commands. Verify ./build-production.sh runs. Open client/src/pages/intelligent-schedule.tsx, ensure handleGenerateVideo calls /api/generate-video-prompt for two ASMR prompts (e.g., ‘ASMR glass cutting, 60s’ and ‘ASMR whisper, 60s’), then /api/posts/:id/generate-video with the prompt, then save. Open server/index.ts, set app.get('/', (req, res) => res.sendFile('dist/index.html', { root: __dirname })); and app.get('/public', (req, res) => res.sendFile('public/index.html', { root: __dirname })) to break the redirect loop, set app.use(express.static('dist', { setHeaders: (res, path) => { if (path.endsWith('.js')) res.set('Content-Type', 'application/javascript'); } })) for MIME, comment out api.sorryapp.com calls (e.g., // fetch('https://api.sorryapp.com/v1/pages/27c7b5fc')) if present, then restart with Restarted Start application. Open server/routes.ts, ensure /api/generate-ai-schedule has a guard (e.g., let attempts = 0; if (attempts++ < 1 && PostQuotaService.hasPostsRemaining(userId)) { /* generate */ }) and calls deductPost(userId, postId) per post, update /api/generate-video-prompt for ASMR prompts, with /api/posts/:id/generate-video running exec('python generate.py --prompt \"${prompt}\" --output video.mp4 --asmr', (err) => { if (!err) res.sendFile('video.mp4'); else res.status(500).send(err); }). Open server/auto-posting-enforcer.ts, ensure enforce publishing respects getRemainingPosts(userId). Ensure server/PostQuotaService.ts enforces a dynamic 30-day cycle. In client/src/pages/intelligent-schedule.tsx, confirm 520 posts display. In test/, run npx tsx test-comprehensive-quota-fix.js for 6/6 pass, testing loop stop, ASMR videos, and post count. In deploy.sh, set to ./build-production.sh && node server/index.js with curl http://localhost:5000/api/health pre-check and 520 posts visible. Document the loop fix, video reinforce, Vite removal, and test results in replit.md, avoiding plugins. Redeploy with ./deploy.sh after 6/6 pass, and manually verify 520 posts and ASMR videos on platforms."