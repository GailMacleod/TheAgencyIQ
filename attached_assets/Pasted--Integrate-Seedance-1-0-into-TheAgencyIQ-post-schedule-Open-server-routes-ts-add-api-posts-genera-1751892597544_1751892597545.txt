"Integrate Seedance 1.0 into TheAgencyIQ post schedule. Open server/routes.ts, add /api/posts/generate with const axios = require('axios'); const semaphore = require('semaphore')(2); app.post('/api/posts/generate', (req, res) => { const { subscription } = req.session; const quota = { basic: 26, pro: 52, premium: 78 }[subscription] || 52; const posts = Array(quota).fill().map(() => ({ content: 'Sample content', video: null })); req.session.posts = posts; res.json(posts); }), and /api/posts/:id/generate-video with if (req.session.videoAttempts?.[req.params.id] >= 2) return res.status(400).send('Max attempts'); semaphore.take(() => { const { prompt } = req.body; axios.post('https://api.wavespeed.ai/api/v3/bytedance/seedance-v1-pro-t2v-720p', { prompt, duration: 30, resolution: '720p' }, { headers: { 'Authorization': Bearer ${process.env.SEEDANCE_API_KEY} } }).then(response => { req.session.videoAttempts = req.session.videoAttempts || {}; req.session.videoAttempts[req.params.id] = (req.session.videoAttempts[req.params.id] || 0) + 1; res.json({ videoUrl: response.data.videoUrl }); }).catch(err => res.status(500).send(err.message)).finally(() => semaphore.leave()); }); }), add /api/posts/:id/approve-video to set req.session.posts[req.params.id].video = req.body.videoUrl; res.sendStatus(200). Open client/src/pages/intelligent-schedule.tsx, keep card popup, ensure ‘Generate Post’ creates posts, add ‘Add Video’ per post triggering handleGenerateVideo with loading (<div className="loading">Generating...</div>), fetch /api/posts/:id/generate-video-prompt returning [{ prompt: 'Growth: Drip, 30s', editable: true }, { prompt: 'Ease: Breeze, 30s', editable: true }] if attempts < 2, preview with <video controls src={videoUrl} />, ‘Approve’ calls /api/posts/:id/approve-video. Open server/auto-posting-enforcer.ts, publish posts with embedded videos. Ensure server/PostQuotaService.ts enforces 30-day cycle. In test/, run npx tsx test-comprehensive-quota-fix.js for 6/6 pass. In deploy.sh, set to ./build-production.sh && node server/index.js with curl http://localhost:5000/api/health. Add .env with SEEDANCE_API_KEY=your_key, install npm install axios semaphore. Document integration and test in replit.md. Redeploy with ./deploy.sh after 6/6 pass, verify posts with videos."